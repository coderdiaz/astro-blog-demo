---
const { href, title, description, target = '_blank' } = Astro.props;
---
<article class="p-5 bg-gray-950 border border-gray-900 rounded-lg text-white flex flex-1 w-full h-full">
  <a href={href} target={target}>
    <div class="flex flex-col gap-2">
      <h3 class="font-medium">{title}</h3>
      <p class="text-gray-400">{description}</p>
    </div>
  </a>
</article>